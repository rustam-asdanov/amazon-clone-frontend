import React from "react";
import { useState } from "react";
import MyPosts from "./MyPosts";

const Header = () => {
    return <header>
        <h2>Nile Shop</h2>
    </header>
  );
};

export default Header;
